import { ExceptionFiltersFilter } from './exception-filters.filter';

describe('ExceptionFiltersFilter', () => {
  it('should be defined', () => {
    expect(new ExceptionFiltersFilter()).toBeDefined();
  });
});
