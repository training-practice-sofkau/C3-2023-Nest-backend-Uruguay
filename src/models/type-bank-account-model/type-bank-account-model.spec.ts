import { TypeBankAccountModel } from './type-bank-account-model';

describe('TypeBankAccountModel', () => {
  it('should be defined', () => {
    expect(new TypeBankAccountModel()).toBeDefined();
  });
});
